import { colors } from '../../styles/theme';

const { RED, BLUE, GREEN } = colors;

export const backgroundColor = new Map(
  [
    ['Delete', RED],
    ['Rent', BLUE],
    ['Cancel rent', RED],
    ['Submit rent', GREEN],
  ]
);